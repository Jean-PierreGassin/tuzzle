/**
 * Promise aggregation utilities for tuzzle — the TypeScript equivalent of
 * `guzzlehttp/promises`. Placeholder export until the promises slice lands.
 */
export const PROMISES_PLACEHOLDER = '@tuzzle/promises' as const;
